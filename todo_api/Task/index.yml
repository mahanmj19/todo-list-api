info:
  name: index
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/tasks?is_done=true"
  params:
    - name: is_done
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
