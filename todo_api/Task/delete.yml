info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/tasks/:id"
  params:
    - name: id
      value: "4"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
