info:
  name: check
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/tasks/3/toggle"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
