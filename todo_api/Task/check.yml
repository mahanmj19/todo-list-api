info:
  name: check
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base_url}}/tasks/:id/toggle"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
