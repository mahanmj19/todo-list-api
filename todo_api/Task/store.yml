info:
  name: store
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/tasks"
  body:
    type: json
    data: |-
      {
          "title": "سومین تسک",
          "description": "توضیحات تسک سوم کوتاه"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
